'use client';

/**
 * TakbisConverter — the self-contained PDF → Excel feature.
 * This component is intentionally isolated so it can be embedded
 * anywhere without coupling to the surrounding dashboard layout.
 *
 * This stub will be fleshed out in Step 4 (full UI + Excel export)
 * after the parse pipeline has been validated with real PDFs.
 */
export { default } from './FileDropzone';
